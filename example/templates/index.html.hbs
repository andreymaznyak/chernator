<!-- THIS IS FILE AUTO GENERATED PLEASE CHANGE index.html.hbs -->
<!-- FOR GENERATE IT RUN: npm run generate-icon-font -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <link rel="stylesheet" href="/icons.css" />
  <title>Icons</title>
</head>
<body>
  <div>
    <h1>Iconography</h1>
    <h1>{{fontName}}</h1>
    <div class="d-flex flex-wrap">
      {{#each names}}
      <div class="p-1 m-1">
        <span class="{{../baseSelector}}">{{this}}</span>
        <span>{{this}}</span>
      </div>
      {{/each}}
    </div>
    <div class="d-flex flex-wrap">
      {{#each names}}
      <div class="m-1">
        <span class="{{../baseSelector}} rounded-circle bg-primary text-white">{{this}}</span>
        <span class="{{../baseSelector}} rounded-circle bg-primary text-white p-1">{{this}}</span>
        <span class="{{../baseSelector}} rounded-circle bg-primary text-white p-2">{{this}}</span>
        <span class="{{../baseSelector}} rounded-circle bg-primary text-white p-3">{{this}}</span>
        <span class="{{../baseSelector}} rounded-circle bg-primary text-white p-4">{{this}}</span>
        <span class="{{../baseSelector}} rounded-circle bg-primary text-white p-5">{{this}}</span>
      </div>
      {{/each}}
    </div>
  </div>
</body>
</html>
  
